spring:
  application:
    name: hack.flux-gitlab-integration
  config:
    import: "optional:configserver:"
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: hackflux-gitlab-integration

hackflux:
  kafka:
    topic: hackflux-events
